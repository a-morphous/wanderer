/**
 * Returns a list of files, as strings with paths relative to the source directory
 * @param dirPath
 * @returns
 */
export declare const readdirSyncRecursive: (sourceDirectory: string) => string[];
